# Keep shell scripts LF so they run on Linux regardless of the checkout platform.
*.sh text eol=lf
# PowerShell scripts are Windows-oriented.
*.ps1 text eol=crlf
# Everything else: normalize to LF in the repo.
* text=auto eol=lf
